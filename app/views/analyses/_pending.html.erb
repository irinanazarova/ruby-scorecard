<article class="step step--pending">
  <header class="step__head">
    <h2><%= title %></h2>
    <span class="step__meta"><span class="tag tag--waiting">waiting…</span></span>
  </header>
  <p class="step__hint"><%= hint %></p>
</article>
