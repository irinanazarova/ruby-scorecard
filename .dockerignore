# nginx image needs only dist/ + deploy/; the Fly fetch image (fetch/Dockerfile) also
# needs scripts/ + data/; the check service (web/check.Dockerfile) needs scripts/ + web/.
*
!dist
!deploy
!scripts
!data
!fetch
!web
